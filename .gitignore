# Oversized scratch experiment — embeds a 52MB riscv64 Node binary (~50MB notebook).
# Kept out of git history deliberately; rebuild recipe lives in
# tools/scratch/claude-emu/ in the parent repo. See project_claude_emu_riscv memory.
notebooks/linux-claude.html
